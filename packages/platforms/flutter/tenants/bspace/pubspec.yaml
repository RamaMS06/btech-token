name: btech_tokens_bspace
description: BTech design tokens for bspace tenant — generated by @btech/design-system
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  btech_tokens:
    path: ../../    # relative to packages/platforms/flutter/tenants/bspace/
