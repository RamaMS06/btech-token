name: btech_tokens_tenant_a
description: BTech design tokens for tenant-a tenant — generated by pnpm generate
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  btech_tokens:
    path: ../../
