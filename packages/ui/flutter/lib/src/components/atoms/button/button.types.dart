// BTButton types — Figma source: node 114:2645.

/// Visual style of the button.
enum BTButtonVariant {
  primary,
  secondary,
  destructive,
  outline,
  ghost,
}

/// Padding scale.
enum BTButtonSize {
  /// Default — 12 px vertical × 16 px horizontal.
  md,

  /// Small — 8 px on all sides.
  sm,
}
