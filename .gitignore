# ── Node / pnpm ───────────────────────────────────────────────────────────────
node_modules/
.pnpm-store/

# ── Build outputs ─────────────────────────────────────────────────────────────
dist/
**/dist/
.turbo/

# ── TypeScript ────────────────────────────────────────────────────────────────
packages/**/src/**/*.js
packages/**/src/**/*.js.map
packages/**/src/**/*.d.ts
*.tsbuildinfo

# ── Dart / Flutter — packages/ ────────────────────────────────────────────────
# pubspec.lock for libraries is intentionally NOT committed (pub resolves at consumer side)
packages/**/pubspec.lock
packages/**/.dart_tool/
packages/**/build/
packages/**/.flutter-plugins
packages/**/.flutter-plugins-dependencies

# ── Root-level Dart tooling ────────────────────────────────────────────────────
.dart_tool/
build/

# ── Env / secrets ─────────────────────────────────────────────────────────────
# .npmrc is committed — it contains only registry config, no secrets.
# Auth tokens are injected at runtime via ${NODE_AUTH_TOKEN} env var.
.env
.env.*
.env.local
.env.*.local

# ── Git worktrees ─────────────────────────────────────────────────────────────
.worktrees/

# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/settings.json
*.swp
*.swo

# ── Demo apps — generated / runtime files only (source files ARE committed) ───
apps/**/.flutter-plugins
apps/**/.flutter-plugins-dependencies
apps/**/.dart_tool/
apps/**/build/
apps/**/.flutter-plugins-dependencies.json

# ── Changesets ────────────────────────────────────────────────────────────────
.changeset/*.js
# Added by code-review-graph
.code-review-graph/
