# @ramaMS06 packages are published to GitHub Packages.
# Consumers must set GITHUB_TOKEN (read:packages scope) in their environment.
@ramaMS06:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
