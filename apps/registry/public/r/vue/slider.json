{
  "$schema": "https://ramaMS06.github.io/btech-token/schema/registry-item.json",
  "name": "slider",
  "type": "registry:ui",
  "title": "BTSlider",
  "description": "Interactive range slider atom. Supports three layout types (default, range, vertical) and three color variants (primary, secondary, destructive). Single-thumb and dual-thumb (range) modes supported.",
  "framework": "vue",
  "category": "atoms",
  "figmaUrl": "https://www.figma.com/design/btech/?node-id=434-7617",
  "figmaNodeId": "434:7617",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "BTSlider.css",
      "target": "@ui/BTSlider.css",
      "type": "registry:ui",
      "content": "/* BTSlider — slider atom (Figma 434:7617)\n *\n * Types:   default (horizontal · 1 thumb)\n *          range   (horizontal · 2 thumbs)\n *          vertical (rotated · 1 thumb)\n *\n * Variants: primary · secondary · destructive\n *\n * Architecture:\n *   - .bt-slider            root container  (position: relative)\n *   - .bt-slider__track     inactive background bar\n *   - .bt-slider__fill      active segment (width/left/right via inline style)\n *   - .bt-slider__input     native <input type=\"range\"> — invisible (opacity:0),\n *                           handles a11y + keyboard + pointer interaction\n *   - .bt-slider__thumb     visual circle positioned via JS\n *   - .bt-slider__tooltip   value callout above the thumb\n */\n\n/* ── CSS custom properties (set per variant) ─────────────────────────────── */\n\n.bt-slider--primary {\n  --bt-slider-active:   var(--color-brand-primary);\n  --bt-slider-inactive: rgba(20, 91, 195, 0.25); /* brand.primary @25% */\n}\n\n.bt-slider--secondary {\n  --bt-slider-active:   #64748b; /* icon.secondary — component-specific */\n  --bt-slider-inactive: rgba(100, 116, 139, 0.25);\n}\n\n.bt-slider--destructive {\n  --bt-slider-active:   #991515; /* icon.error — component-specific */\n  --bt-slider-inactive: rgba(153, 21, 21, 0.25);\n}\n\n/* ── Root ────────────────────────────────────────────────────────────────── */\n\n.bt-slider {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  touch-action: none;\n}\n\n.bt-slider--horizontal {\n  width: 100%;\n  height: 24px; /* thumb diameter */\n}\n\n.bt-slider--vertical {\n  flex-direction: column;\n  width: 27px;   /* Figma: w-[27px] */\n  height: 200px; /* Figma: h-[200px] */\n  justify-content: center;\n}\n\n/* ── Track ───────────────────────────────────────────────────────────────── */\n\n.bt-slider__track {\n  position: absolute;\n  border-radius: 9999px;\n  background: var(--bt-slider-inactive);\n  overflow: visible;\n}\n\n.bt-slider--horizontal .bt-slider__track {\n  left: 12px; /* thumb radius — thumb doesn't travel past edges */\n  right: 12px;\n  height: 4px;\n}\n\n.bt-slider--vertical .bt-slider__track {\n  top: 12px;\n  bottom: 12px;\n  width: 4px;\n  left: 50%;\n  transform: translateX(-50%);\n}\n\n/* ── Fill ────────────────────────────────────────────────────────────────── */\n\n.bt-slider__fill {\n  position: absolute;\n  background: var(--bt-slider-active);\n  border-radius: 9999px;\n}\n\n.bt-slider--horizontal .bt-slider__fill {\n  top: 0;\n  bottom: 0;\n  /* left + right set via inline style */\n}\n\n.bt-slider--vertical .bt-slider__fill {\n  left: 0;\n  right: 0;\n  bottom: 0;\n  /* height set via inline style */\n}\n\n/* ── Native input (invisible — interaction only) ─────────────────────────── */\n\n.bt-slider__input {\n  position: absolute;\n  opacity: 0;\n  cursor: pointer;\n  margin: 0;\n  z-index: 3;\n}\n\n.bt-slider--horizontal .bt-slider__input {\n  inset: 0;\n  width: 100%;\n  height: 100%;\n}\n\n/* Two-input range: stack on same dimensions */\n.bt-slider__input--start,\n.bt-slider__input--end {\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  pointer-events: none; /* managed per-thumb via JS */\n}\n\n.bt-slider__input--start {\n  pointer-events: all; /* always interactive on left side */\n}\n\n.bt-slider--vertical .bt-slider__input {\n  /* Rotate so it runs bottom→top */\n  width: 200px;\n  height: 27px;\n  top: 0;\n  left: 50%;\n  transform: rotate(-90deg) translateX(-100%);\n  transform-origin: left top;\n  /* Invert: value=100 at top, value=0 at bottom */\n}\n\n/* ── Visual thumb ────────────────────────────────────────────────────────── */\n\n.bt-slider__thumb {\n  position: absolute;\n  width: 24px;\n  height: 24px;\n  border-radius: 50%;\n  background: var(--bt-slider-active);\n  border: 2.5px solid white;\n  box-shadow: 0 1px 6px rgba(0, 0, 0, 0.18);\n  pointer-events: none; /* input handles interaction */\n  z-index: 2;\n  transform: translateX(-50%);\n  transition: box-shadow 0.15s;\n}\n\n.bt-slider--vertical .bt-slider__thumb {\n  transform: translate(-50%, 50%);\n  left: 50%;\n  /* bottom set via inline style */\n}\n\n.bt-slider__input:focus-visible ~ .bt-slider__thumb {\n  box-shadow: 0 0 0 4px rgba(20, 91, 195, 0.2);\n}\n\n/* ── Tooltip ─────────────────────────────────────────────────────────────── */\n\n/* Figma 434:7544 — dark pill (bg.inverse #292f37) + 16×8px downward arrow */\n.bt-slider__tooltip {\n  position: absolute;\n  background: #292f37;   /* bg.inverse */\n  color: #ffffff;        /* text.inverse */\n  font-family: var(--typography-font-family-sans);\n  font-size: 14px;       /* fontSize.sm */\n  font-weight: 500;\n  line-height: 16px;     /* lineHeight.xs */\n  padding: 8px 12px;     /* spacing.sm × spacing.md */\n  border-radius: 8px;    /* radius.sm */\n  white-space: nowrap;\n  pointer-events: none;\n  z-index: 10;\n}\n\n/* Downward arrow — 16×8px (Figma 434:7544) */\n.bt-slider__tooltip::after {\n  content: '';\n  position: absolute;\n  bottom: -8px;\n  left: 50%;\n  transform: translateX(-50%);\n  border-left: 8px solid transparent;\n  border-right: 8px solid transparent;\n  border-top: 8px solid #292f37;\n  border-bottom: none;\n}\n\n/* Horizontal: tooltip pill sits 2px above thumb top.\n * Container height = 24px (= thumb ⌀).\n * bottom = 24px + arrow(8px) + gap(2px) = 34px = calc(100% + 10px).\n * Arrow tip ends up at -2px from container top = 2px above thumb. */\n.bt-slider--horizontal .bt-slider__tooltip {\n  bottom: calc(100% + 10px);\n  top: auto;\n  transform: translateX(-50%);\n  /* left set inline: calc(ratio * (100% - 24px) + 12px) */\n}\n\n/* Vertical: tooltip pill to the left, centered on thumb.\n * bottom (inline) = ratio * (100% - 24px)  [no +12px — translateY(50%) handles centering]\n * Arrow points right toward the thumb. */\n.bt-slider--vertical .bt-slider__tooltip {\n  top: auto;\n  left: auto;\n  right: calc(100% + 6px);\n  transform: translateY(50%);\n}\n\n/* Right-pointing arrow for vertical (6×12px) */\n.bt-slider--vertical .bt-slider__tooltip::after {\n  bottom: auto;\n  left: auto;\n  top: 50%;\n  right: -6px;\n  transform: translateY(-50%);\n  border-top: 6px solid transparent;\n  border-bottom: 6px solid transparent;\n  border-left: 6px solid #292f37;\n  border-right: none;\n}\n\n/* ── alwaysShown: false — tooltip visible only on hover / focus ───────────── */\n\n.bt-slider--tooltip-hover .bt-slider__tooltip {\n  opacity: 0;\n  transition: opacity 0.15s;\n}\n\n.bt-slider--tooltip-hover:hover .bt-slider__tooltip,\n.bt-slider--tooltip-hover:has(.bt-slider__input:focus-visible) .bt-slider__tooltip {\n  opacity: 1;\n}\n\n/* ── Disabled ────────────────────────────────────────────────────────────── */\n\n.bt-slider--disabled {\n  opacity: 0.45;\n  pointer-events: none;\n}\n"
    },
    {
      "path": "BTSlider.types.ts",
      "target": "@ui/BTSlider.types.ts",
      "type": "registry:ui",
      "content": "// BTSlider types — Figma source: node 434:7617.\n\n/** Layout and thumb count. */\nexport type BTSliderType = 'default' | 'range' | 'vertical';\n\n/**\n * Color variant of the active track + thumb.\n * - primary     → brand blue  (#145bc3)\n * - secondary   → neutral     (#64748b)\n * - destructive → error red   (#991515)\n */\nexport type BTSliderVariant = 'primary' | 'secondary' | 'destructive';\n\nexport interface BTSliderProps {\n  /**\n   * Layout type.\n   * - 'default'  → horizontal, single thumb\n   * - 'range'    → horizontal, two thumbs (start + end)\n   * - 'vertical' → vertical, single thumb\n   * @default 'default'\n   */\n  type?: BTSliderType;\n  /**\n   * Color variant for active track and thumb.\n   * @default 'primary'\n   */\n  variant?: BTSliderVariant;\n  /** Controlled value for default / vertical types. */\n  value?: number;\n  /** Controlled start value for range type. */\n  startValue?: number;\n  /** Controlled end value for range type. */\n  endValue?: number;\n  /** Minimum selectable value. @default 0 */\n  min?: number;\n  /** Maximum selectable value. @default 100 */\n  max?: number;\n  /** Increment step. @default 1 */\n  step?: number;\n  /** Show value tooltip above (or beside for vertical) the thumb. @default true */\n  showTooltip?: boolean;\n  /**\n   * When `true` the tooltip is always visible (default).\n   * When `false` the tooltip only appears on hover / keyboard focus.\n   * Has no effect when `showTooltip` is `false`.\n   * @default true\n   */\n  alwaysShown?: boolean;\n  /** Disables all interaction. @default false */\n  disabled?: boolean;\n  /** Additional CSS class names. */\n  className?: string;\n}\n"
    },
    {
      "path": "BTSlider.vue",
      "target": "@ui/BTSlider.vue",
      "type": "registry:ui",
      "content": "<!-- BTSlider — interactive range slider atom (Figma 434:7617).\n *\n * Three layout types: 'default' (horizontal · 1 thumb), 'range'\n * (horizontal · 2 thumbs), 'vertical' (1 thumb).\n * Three color variants: 'primary' · 'secondary' · 'destructive'.\n *\n * Example:\n * ```vue\n * <BTSlider v-model:value=\"brightness\" />\n * <BTSlider type=\"range\" v-model:startValue=\"from\" v-model:endValue=\"to\" />\n * <BTSlider type=\"vertical\" variant=\"destructive\" v-model:value=\"level\" />\n * ```\n-->\n<script setup lang=\"ts\">\nimport './BTSlider.css';\nimport { computed, ref, watch } from 'vue';\nimport type { BTSliderProps } from './BTSlider.types';\n\nconst props = withDefaults(defineProps<BTSliderProps>(), {\n  type: 'default',\n  variant: 'primary',\n  min: 0,\n  max: 100,\n  step: 1,\n  showTooltip: true,\n  alwaysShown: true,\n  disabled: false,\n});\n\nconst emit = defineEmits<{\n  (e: 'update:value', v: number): void;\n  (e: 'update:startValue', v: number): void;\n  (e: 'update:endValue', v: number): void;\n}>();\n\n// ── Internal state (uncontrolled fallback) ────────────────────────────────\nconst _value = ref(props.value ?? Math.round((props.min + props.max) / 2));\nconst _start = ref(props.startValue ?? props.min + Math.round((props.max - props.min) * 0.2));\nconst _end   = ref(props.endValue   ?? props.max - Math.round((props.max - props.min) * 0.2));\n\nwatch(() => props.value,      (v) => { if (v !== undefined) _value.value = v; });\nwatch(() => props.startValue, (v) => { if (v !== undefined) _start.value = v; });\nwatch(() => props.endValue,   (v) => { if (v !== undefined) _end.value   = v; });\n\nconst val   = computed(() => props.value      ?? _value.value);\nconst start = computed(() => props.startValue ?? _start.value);\nconst end   = computed(() => props.endValue   ?? _end.value);\n\n// ── Helpers ───────────────────────────────────────────────────────────────\nfunction toRatio(v: number) {\n  return (v - props.min) / (props.max - props.min);\n}\nfunction clamp(v: number) {\n  return Math.min(props.max, Math.max(props.min, v));\n}\n\n// Horizontal thumb / tooltip left (accounts for thumb radius so thumb\n// center travels from 12 px to containerWidth − 12 px).\nfunction hLeft(ratio: number) {\n  return `calc(${ratio} * (100% - 24px) + 12px)`;\n}\n\n// ── Derived styles ────────────────────────────────────────────────────────\nconst fillStyle = computed(() => {\n  if (props.type === 'range') {\n    return { left: `${toRatio(start.value) * 100}%`, right: `${(1 - toRatio(end.value)) * 100}%` };\n  }\n  if (props.type === 'vertical') {\n    return { height: `${toRatio(val.value) * 100}%` };\n  }\n  return { left: '0%', right: `${(1 - toRatio(val.value)) * 100}%` };\n});\n\nconst thumbStyle = computed(() => ({\n  left: hLeft(toRatio(val.value)),\n}));\nconst thumbStartStyle = computed(() => ({\n  left: hLeft(toRatio(start.value)),\n}));\nconst thumbEndStyle = computed(() => ({\n  left: hLeft(toRatio(end.value)),\n}));\n\n// Vertical thumb: bottom position\nconst vThumbStyle = computed(() => ({\n  bottom: `calc(${toRatio(val.value)} * (100% - 24px))`,\n  left: '50%',\n}));\nconst vTooltipStyle = computed(() => ({\n  // +12px removed — translateY(50%) already centers tooltip on thumb center\n  bottom: `calc(${toRatio(val.value)} * (100% - 24px))`,\n}));\n\nconst tooltipStyle   = computed(() => ({ left: hLeft(toRatio(val.value)) }));\nconst ttStartStyle   = computed(() => ({ left: hLeft(toRatio(start.value)) }));\nconst ttEndStyle     = computed(() => ({ left: hLeft(toRatio(end.value)) }));\n\n// ── Events ────────────────────────────────────────────────────────────────\nfunction onValueInput(e: Event) {\n  const v = clamp(Number((e.target as HTMLInputElement).value));\n  _value.value = v;\n  emit('update:value', v);\n}\n\nfunction onStartInput(e: Event) {\n  const v = clamp(Math.min(Number((e.target as HTMLInputElement).value), end.value - props.step));\n  _start.value = v;\n  emit('update:startValue', v);\n}\n\nfunction onEndInput(e: Event) {\n  const v = clamp(Math.max(Number((e.target as HTMLInputElement).value), start.value + props.step));\n  _end.value = v;\n  emit('update:endValue', v);\n}\n\n// Range: raise z-index of whichever thumb is on the right so user can\n// drag it leftward past the other thumb.\nconst startZ = computed(() => (toRatio(start.value) > 0.9 ? 4 : 3));\nconst endZ   = computed(() => (toRatio(end.value)   < 0.1 ? 4 : 3));\n</script>\n\n<template>\n  <div\n    :class=\"[\n      'bt-slider',\n      type === 'vertical' ? 'bt-slider--vertical' : 'bt-slider--horizontal',\n      `bt-slider--${variant}`,\n      disabled && 'bt-slider--disabled',\n      showTooltip && !alwaysShown && 'bt-slider--tooltip-hover',\n      className,\n    ]\"\n  >\n    <!-- ── Default / Vertical (single thumb) ─────────────────────────── -->\n    <template v-if=\"type !== 'range'\">\n      <!-- Tooltip -->\n      <div\n        v-if=\"showTooltip\"\n        class=\"bt-slider__tooltip\"\n        :style=\"type === 'vertical' ? vTooltipStyle : tooltipStyle\"\n      >\n        {{ val }}\n      </div>\n\n      <!-- Track -->\n      <div class=\"bt-slider__track\">\n        <div class=\"bt-slider__fill\" :style=\"fillStyle\" />\n      </div>\n\n      <!-- Native input (invisible, handles interaction) -->\n      <input\n        class=\"bt-slider__input\"\n        type=\"range\"\n        :min=\"min\"\n        :max=\"max\"\n        :step=\"step\"\n        :value=\"val\"\n        :disabled=\"disabled\"\n        :aria-orientation=\"type === 'vertical' ? 'vertical' : 'horizontal'\"\n        :aria-valuemin=\"min\"\n        :aria-valuemax=\"max\"\n        :aria-valuenow=\"val\"\n        @input=\"onValueInput\"\n      />\n\n      <!-- Visual thumb -->\n      <div\n        class=\"bt-slider__thumb\"\n        :style=\"type === 'vertical' ? vThumbStyle : thumbStyle\"\n      />\n    </template>\n\n    <!-- ── Range (two thumbs) ─────────────────────────────────────────── -->\n    <template v-else>\n      <!-- Tooltips -->\n      <div v-if=\"showTooltip\" class=\"bt-slider__tooltip\" :style=\"ttStartStyle\">{{ start }}</div>\n      <div v-if=\"showTooltip\" class=\"bt-slider__tooltip\" :style=\"ttEndStyle\">{{ end }}</div>\n\n      <!-- Track -->\n      <div class=\"bt-slider__track\">\n        <div class=\"bt-slider__fill\" :style=\"fillStyle\" />\n      </div>\n\n      <!-- Start input -->\n      <input\n        class=\"bt-slider__input bt-slider__input--start\"\n        type=\"range\"\n        :style=\"{ zIndex: startZ }\"\n        :min=\"min\"\n        :max=\"max\"\n        :step=\"step\"\n        :value=\"start\"\n        :disabled=\"disabled\"\n        aria-label=\"Range start\"\n        @input=\"onStartInput\"\n      />\n      <!-- End input -->\n      <input\n        class=\"bt-slider__input bt-slider__input--end\"\n        type=\"range\"\n        :style=\"{ zIndex: endZ }\"\n        :min=\"min\"\n        :max=\"max\"\n        :step=\"step\"\n        :value=\"end\"\n        :disabled=\"disabled\"\n        aria-label=\"Range end\"\n        @input=\"onEndInput\"\n      />\n\n      <!-- Visual thumbs -->\n      <div class=\"bt-slider__thumb\" :style=\"thumbStartStyle\" />\n      <div class=\"bt-slider__thumb\" :style=\"thumbEndStyle\" />\n    </template>\n  </div>\n</template>\n"
    }
  ]
}